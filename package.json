{
  "name": "voice-chat-signaling",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build step required'",
    "vercel-build": "echo 'No build step required'",
    "start": "node api/signaling.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": "18.x"
  }
}
